[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

[functions.generate-rebuttal]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
